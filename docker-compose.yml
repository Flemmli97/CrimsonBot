services:
  patreon_linker:
    build: .
    restart: always
    container_name: CrimsonBot
    environment:
      - DATA=/bot/data # The bots data directory inside the container
      - CONFIG=./config.yml # The bots config file relative to the DATA directory
    volumes:
      - ./data:/bot/data
